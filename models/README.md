# Models

`final_membrane_model` is the trained Cellpose membrane model used by the inference example in the main README.

Additional trained models are ignored by default to keep the repository small and reviewable.
